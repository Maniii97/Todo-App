const Header = () => {
  return <nav>Todo Application</nav>
}

const TodoText = () => {
  return <h1 className="TodoText">Not just add your goals but make sure to finish them.</h1>
}


export { TodoText , Header }